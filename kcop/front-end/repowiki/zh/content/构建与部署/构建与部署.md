# 构建与部署

## 构建入口

当前项目使用 Grunt，不使用 Vite。

- `Gruntfile.js`：主构建配置。
- `build.bat`：默认构建。
- `build-all.bat`：全量构建/发布类脚本。
- `build-tpls.bat`：模板编译。

## 产物目录

- `dist/`：构建中间/前端产物。
- `../../kcop-dist/`：默认任务中的发布拷贝目标。

默认任务包含 clean、copy、cssmin、transport、concat、uglify、replace、copy:build、copy:report 等步骤。
