# 客户识别模块

客户识别能力分布在 `apps/opp/common`、`apps/opp/operator/common` 和 `basic/service/cust-service` 等模块中。

常见模式：通过 URL 参数判断是否已有 `CUST_CODE`；没有客户号时加载客户识别组件；识别后用 `utils.buildUrl(location.href, custInfo)` 跳转回当前页。
