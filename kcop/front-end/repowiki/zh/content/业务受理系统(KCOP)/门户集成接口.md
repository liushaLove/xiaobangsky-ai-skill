# 门户集成接口

业务页面通过 HTML 引入 `loader.js` 并声明 `data-main` 集成到门户。

`kcopTop` 指向顶层窗口，`kcopTop.kui` 共享 KUI 基础对象和缓存。iframe 页面打开、关闭、刷新由 `frame/views/content-view.js`、`tabs-view.js` 等管理。页面参数通过 URL 传递，使用 `utils.getUrlParam()` 读取。
