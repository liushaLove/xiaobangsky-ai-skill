# 开发工具

## 搜索

```powershell
rg -n "关键字" apps basic frame
rg --files | rg "reset-cust-org"
```

## 构建工具

- Grunt：主构建工具。
- `build.bat`：默认构建入口。
- `build-tpls.bat`：业务模板编译。
- `build-all.bat`：全量发布类脚本。

项目依赖 legacy Node/Grunt/node_modules 环境。不要默认执行依赖升级。
