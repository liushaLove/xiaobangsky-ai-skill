# 路由系统

当前项目没有 Vue Router。页面跳转由 URL、菜单数据、iframe/标签页容器和 loader 共同完成。

1. 菜单数据给出页面 URL 或模块信息。
2. `content-view.js` / 入口页打开页面。
3. HTML 中的 `loader.js` 根据 `data-main` 加载主 JS。
4. 主 JS 执行 `exports.$ready`。

页面参数通过 `utils.getUrlParam()` 读取，拼接跳转 URL 可用 `utils.buildUrl()`。
