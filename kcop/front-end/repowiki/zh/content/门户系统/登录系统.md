# 登录系统

登录相关逻辑主要在 `apps/opp/pages/src/index.js` 和 `apps/opp/frame` 相关模块中，依赖 `ajax.login`、`kui.login`、`memo`、native 信息采集等能力。

登录数据写入 `kui.basic.loginData` 或顶层共享对象。请求时 `frame/core/ajax.js` 会补充 session/token 和操作员信息。会话超时由 `ajax.showSessionTimeoutPrompt` 统一提示。

不要按 Vue Router 登录守卫思路处理。
